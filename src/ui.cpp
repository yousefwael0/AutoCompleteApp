#include "ui.h"
#include <iostream>

void UI::displaySuggestions(const std::vector<std::string>& suggestions, const std::string& prefix, const FrequencyManager& freqManager) {
    // Display suggestions
    // Highlight if exact match
}